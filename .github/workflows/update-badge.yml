name: Update Profile Views Badge

on:
  # Uruchamiaj co godzinę
  schedule:
    - cron: '0 * * * *'
  
  # Pozwól na ręczne uruchomienie
  workflow_dispatch:
  
  # Uruchamiaj przy push do main (dla testów)
  push:
    branches:
      - main

jobs:
  update-badge:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
      
      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: 📊 Generate and upload badge to Gist
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GIST_ID: ${{ secrets.GIST_ID }}
        run: |
          echo "🚀 Generowanie badge i aktualizacja Gist..."
          node generate-badge.js
          
          if [ $? -eq 0 ]; then
            echo "✅ Badge zaktualizowany w Gist!"
          else
            echo "❌ Wystąpił błąd podczas aktualizacji"
            exit 1
          fi